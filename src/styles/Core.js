import EStyleSheet from 'react-native-extended-stylesheet';
import { BrandPalette as c } from '../styles';

export const Core = EStyleSheet.create({
    headerText: {
        fontSize: '1.2rem',
        color: c.primary,
    }
});
